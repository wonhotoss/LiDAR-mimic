# LiDAR-mimic
이 프로젝트는 일반 3d 렌더러로부터 라이다 영상 특유의 패턴이 있는 포인트클라우드 비주얼을 끌어내는 것을 목표로 한다. 이 과정에서 실제 포인트 클라우드 데이터를 raycast등의 방법으로 생산하지 않으며, 렌더파이프라인 상에서 시각적으로 연출한다. 더 적은 메모리와 cpu bandwidth를 사용하지만, 더 configurable 한 포인트클라우드 like 한 시각연출 방법을 모색한다.
- 패턴을 조사하는 라이다의 위치와 방향, 라이다의 레이저 조사 패턴의 밀도와 모양 등을 실시간으로 조정할 수 있다. 
- 실시간으로 오브젝트의 포인트클라우드 렌더링 / 일반 렌더링을 토글할 수 있어야 한다.
- 오브젝트는 이동, 본 애니메이션 등을 할 수 있다.
- 일반 렌더러로 렌더되는 오브젝트와 라이다 렌더러로 렌더되는 오브젝트간의 차폐 결과가 자연스럽게 연출되어야 한다. 
- 씬에는 opaque 오브젝트만 존재한다.

## 개요
- compute shader + (쉐도우맵 / 데칼 렌더링의 방법을 변용). 오브젝트의 라이다 시점 조사 결과는 라이다 프로젝션 공간 좌표 값으로 GPU버퍼(unity structured buffer)에 저장되고, 이것을 메인 씬 위에 그대로 렌더링. 버퍼 readback 없이 GPU 내에서 처리한다.

## 방법
1. 준비
    - 모든 오브젝트는 ID는 0보다 큰 순차 정수이다.ID 범위는 매우 클 필요는 없다.
    - 어플리케이션은 어떤 오브젝트가 일반 오브젝트이고 어떤 오브젝트가 pc오브젝트인지 구분할 수 있는 기준을 가지며, 이것을 편집할 수 있는 인터페이스를 갖는다. 하나의 오브젝트가 일반과 pc 속성을 모두 가질 수 있다.
    - 라이다는 기본적으로 3d 카메라이며, 위치와 각도를 갖는다. 라이다의 조사 패턴은 라이다에서 출발하는 하나하나의 ray를 의미하는 buffer이다. 라이다의 위치와 각도는 정해져 있으므로, 이 버퍼는 라이다 기준 프로젝션 공간의 xy이다. 이하에서 이것을 pc버퍼라고 부른다.
    - PC 버퍼는 xyz와 더불어 오브젝트 ID 필드를 갖는다.
2. 일반 오브젝트 패스
    - 기본 카메라는 opaque 패스에서 일반오브젝트만 렌더링한다.    
3. pc 패스
    - 라이다 카메라는 depth buffer와 렌더 타겟을 준비한다. 렌더 타겟엔 오브젝트별 ID가 기록되어야 한다.
    - depth buffer + 렌더 타겟을 클리어한다.farthest depth + ID(0)로 클리어.
    - 렌더 타겟에 모든 오브젝트를 렌더링한다. 각 픽셀에는 유래한 오브젝트의 ID가 기록되어야 한다. 
    - compute 쉐이더를 통해 pc버퍼의 z를 갱신한다. 이 과정에서 앞서 만든 depth버퍼 + ID텍스춰를 사용한다. pc 버퍼에는 프로젝션 공간 xy가 저장되어 있으므로, 이것을 uv로 바꿔 뎊스 텍스춰를 샘플링하면 각 ray가 씬과 만나는 지점의 라이다 기준 z를 얻을 수 있다.
    - 위의 과정에서 오브젝트 ID 또한 pc버퍼에 기록된다. 샘플링 과정에서 필터링되지 않도록 주의.
    - 이 과정의 결과로 라이다 프로젝션 공간 기준 포인트클라우드의 좌표와 유래한 오브젝트 ID가 모두 얻어진다.
4. 통합 패스
    - 기본 카메라는 메인 렌더타겟에 pc 버퍼를 일반 포인트클라우드 렌더링 방식으로 렌더링한다. pc 버퍼에는 라이다 프로젝션 기준 pc 좌표가 들어 있다. 렌더링 시점에 이것을 월드 좌표로 다시 변환한 후 메인 카메라로 렌더링한다. 뎊스 읽기 쓰기 모두 on
    - 렌더링 시점에 pc 오브젝트들의 ID목록이 주어진다. 목록과 포인트별 ID를 비교해서 pc오브젝트에 포함된 포인트만 렌더링된다.
    - 각 포인트는 카메라 거리와 무관하게 일정한 크기의 사각형. 카메라 방향으로 살짝 bias해서 both 오브젝트의 depth-fighting을 경감시킨다.
    - 오브젝트 ID별 포인트 색깔과 크기가 별도의 쉐이더 상수나 compute버퍼로 주어지고, 이것을 참고한다.