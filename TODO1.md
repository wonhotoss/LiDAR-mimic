# LiDAR-mimic
이 프로젝트는 일반 3d 렌더러로부터 라이다 영상 특유의 패턴이 있는 포인트클라우드 비주얼을 끌어내는 것을 목표로 한다. 이 과정에서 실제 포인트 클라우드 데이터를 raycast등의 방법으로 생산하지 않으며, 렌더파이프라인 상에서 시각적으로 연출한다. 더 적은 메모리와 cpu bandwidth를 사용하지만, 더 configurable 한 포인트클라우드 like 한 시각연출 방법을 모색한다.
- 패턴을 조사하는 라이다의 위치와 방향, 라이다의 레이저 조사 패턴의 밀도와 모양 등을 실시간으로 조정할 수 있다. 
- 실시간으로 오브젝트의 포인트클라우드 렌더링 / 일반 렌더링을 토글할 수 있어야 한다.
- 오브젝트는 이동, 본 애니메이션 등을 할 수 있다.
- 일반 렌더러로 렌더되는 오브젝트와 라이다 렌더러로 렌더되는 오브젝트간의 차폐 결과가 자연스럽게 연출되어야 한다. 
- 씬에는 opaque 오브젝트만 존재한다.

## 개요
- 쉐도우맵 / 데칼 렌더링의 방법을 변용. 오브젝트의 라이다 시점 레이저 조사 결과를 별도의 맵에 캐시하고, 이것을 실제 씬 렌더링 결과에 augment한다.

## 방법
1. 준비
    - 포인트클라우드로 렌더링되는 오브젝트와 일반 오브젝트는 태그 등으로 구분되어야 한다. 이하에서 '일반오브젝트'/'pc오브젝트' 로 구분한다.
    - 라이다는 기본적으로 3d 카메라이며, 위치와 각도를 갖는다. 라이다의 조사 패턴은 마스크 이미지로 표현된다. 일반적으로 이미지 중심을 기준으로 동심원 모양으로 분포된 작은 원(점)들이다. 이것은 라이다 위치에서 뷰프러스텀을 향해 방사되는 레이저를 의미한다. 이하에서 '라이다 마스크'로 부른다. 1채널이면 된다. 0 배경에, 레이저 조사부분은 1.
2. 일반 오브젝트 패스
    - 기본 카메라는 opaque 패스에서 일반오브젝트만 렌더링한다.
    - 이 과정에서 장면의 depth텍스춰가 렌더링되어야 한다. 추후에 사용된다.
3. 라이다 맵 패스
    - 라이다 카메라는 별도의 렌더 타겟에 모든 오브젝트를 렌더링한다.
        - 일반 오브젝트는 검은색.
        - pc 오브젝트는 픽셀 쉐이더에서 프로젝션 공간 좌표를 기준으로 라이다 마스크를 샘플링한 색깔로 렌더링된다. 결국 라이다 마스크가 데칼처럼 입혀진 형태이다.    
    - 이 과정에서 장면의 depth텍스춰가 렌더링되어야 한다. 추후에 사용된다.
    - 이 과정의 결과(color + depth)를 이하에서 '라이다 맵'이라고 부른다. 컬러 채널 두 개(r: 라이다 마스크 값, g: depth)만 사용하는 텍스춰 한 장으로 표현할 수 있다.
4. 통합 패스
    - 기본 카메라는 별도의 렌더타겟에 pc오브젝트만 렌더링한다. 뎊스 읽기 쓰기 off + 양면 렌더링
        - 픽셀 쉐이더에서 일반 오브젝트 패스의 depth 텍스춰를 샘플링한다. 다른 오브젝트에 가려질 픽셀이라면 discard하거나 alpha=0     
        - 픽셀 쉐이더에서 픽셀의 월드 포지션을 라이다 카메라의 뷰프로젝션 행렬에 곱해서 라이다 기준 프로젝션 공간 좌표를 얻는다. 
        - 이 때 얻어진 깊이가 라이다 맵에 기록된 깊이(g)보다 깊다면 라이다 입장에서 가려진 면이다. discard하거나 alpha=0
        - 그렇지 않다면 라이다 맵에서 얻어진 색깔(r)을 그대로 사용한다. rgba = (r, r, r, r)
        - 결과적으로 라이다에서 보이는 부분에 라이다 마스크가 데칼처럼 입혀진 모습이 된다. 라이다 입장에서의 앞 / 뒷면, 오브젝트 차폐가 고려되지만, 기본 카메라 입장에서는 일반 오브젝트와의 오브젝트 차폐만 고려되고 앞 / 뒷면은 고려되지 않는다.
    - (optional + 미정) 앞선 결과를 pixelize한다. 라이다 마스크 상의 한 점이 최종 결과 상에서 언제나 같은 크기의 점으로 매핑되어야 하는데, 실제로는 오브젝트와 메인 카메라 거리에 따라 크리가 달라지고 차폐에 따라 반만 나온다거나 할 수도 있다. 이 부분으 ㄴ추후에 고민.
    - 앞선 결과를 일반 패스의 메인 렌더 타겟 위에 알파블렌딩한다. 
        

